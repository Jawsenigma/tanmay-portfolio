[build]
  command = "pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# @netlify/plugin-nextjs is auto-detected; pinning explicitly for clarity.
[[plugins]]
  package = "@netlify/plugin-nextjs"
